//Program To find the square root of a number 

#include <stdio.h>
#include <math.h>
int main(){
    float n;
    printf("Enter a number: ");
    scanf("%f", n);
    printf("The Square root of the entered number is: %f", sqrt(n));
    return 0;
}