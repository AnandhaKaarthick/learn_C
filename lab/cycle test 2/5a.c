#include<stdio.h>

int main(){
    char str;
    printf("Enter the string: ");
    scanf("%s", &str);
    for(int i = 0; i<n; i++){
        if(str[i] = ' '){
            str[i] = '\0';
        }
    }
    printf("%s", str);
}