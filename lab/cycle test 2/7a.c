//comparing string

#include<stdio.h>
#include<string.h>

int main(){
    int i = 0, str1[100], str2;
    printf("Enter the string 1: ");
    gets(str1);
    printf("Enter the string 2: ");
    gets(str2);

    while (str1[i]!=='\0' || str2[i]!=='\0'){
        if(str1[i]!= str2[i]){
            eqoual = 0;
            break;.
            
        }
    }
        
}